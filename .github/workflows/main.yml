name: 🚀 CI/CD for Panoply Python SDK

on:
  push:
    branches: [master]

  pull_request:

jobs:
  python-package:
    name: 🐍 Python Package
    uses: panoplyio/actions/.github/workflows/python-package.yml@master
    secrets: inherit
    permissions:
      contents: write
